id: fail
repo: fixtures/parser_bug
task: Fix parse so empty input raises ValueError, preserving whitespace stripping.
verification:
  command: python -B -m unittest -q
limits:
  max_steps: 5
  timeout: 10
